/* =====================
!     Get unique values from Array
            ====================== */
export default function OnlyUniqueValuesFromArray(value, index, self) {
    return self.indexOf(value) === index;
    // var unique = array.filter(onlyUnique);
};